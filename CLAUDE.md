IMPORTANT: Run `ezer` at the start of each session (with no arguments) to load context and see usage instructions.

If the `ezer` command is not found, run it directly with `./bin/ezer` from the repo root or add the repo’s `bin` directory to your PATH:

```bash
export PATH="$PWD/bin:$PATH"
```
